name: Deploy Cloud Functions

on:
  push:
    branches:
      - '**'


jobs:
  list-functions:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        function:
          - FUNCTION_NAME: Task

          - FUNCTION_NAME: Gcp
            
          - FUNCTION_NAME: App
          

    steps:
     - name: check 
       run: echo "${{ toJson(matrix) }}"
     
